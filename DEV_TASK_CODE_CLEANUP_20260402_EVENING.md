# 代码清理报告

**任务**: 分析并清理 src/lib/ 目录结构

**日期**: 2026-04-02

---

## 📊 统计摘要

| 指标 | 数值 |
|------|------|
| 总文件数 | 629 |
| 子目录数 | 51 |
| TypeScript 文件 | 629 |
| 测试文件 | ~150+ |

---

## 🔍 发现的问题

### 1. 空目录

```
src/lib/__mocks__      (空目录)
src/lib/alerting/__tests__  (空目录)
```

**建议**: 删除这两个空目录

### 2. 可能的死代码 - 错误处理模块

| 文件 | 行数 | 状态 |
|------|------|------|
| `error-handler.ts` | 8,875 | ⚠️ 未被导入 |
| `error-handling.ts` | 3,863 | ⚠️ 未被导入 (仅做重导出) |
| `global-error-handlers.ts` | 4,958 | ⚠️ 未被导入 |

**分析**:
- `error-handling.ts` 内容仅为: `export { setupGlobalErrorHandlers, setupBrowserErrorHandlers } from './global-error-handlers'`
- 以上三个文件在代码库中均无导入记录

**建议**: 需要确认是否为遗留代码，如确认无用则删除

### 3. 错误/错误集的潜在混淆

项目中有两套错误模块:

| 目录 | 用途 | 状态 |
|------|------|------|
| `@/lib/errors/` | 统一错误系统 (被使用) | ✅ 活跃 |
| `@/lib/error/` | 另一个错误系统 | ✅ 有限使用 |

**观察**: 两个目录共存可能造成理解混淆，建议后续统一

### 4. 重复文件名的模式

有 50+ 个文件在多个目录中使用相同名称:

```
index.ts      (多个目录)
types.ts     (多个目录)
utils.ts     (多个目录)
cache.ts     (多个目录)
error.ts     (多个目录)
...
```

**分析**: 这是正常情况（模块化设计），无需修改

### 5. utils 模块分析

| 模块 | 用途 | 使用情况 |
|------|------|----------|
| `utils.ts` | 核心工具 (cn, format等) | ✅ 高频使用 |
| `utils/` | 子工具集 (async, retry等) | 有限使用 |

**状态**: 分离合理，两个模块服务不同目的

---

## 🧹 可执行清理

### 高优先级

1. **删除空目录**:
   ```bash
   rm -rf src/lib/__mocks__
   rm -rf src/lib/alerting/__tests__
   ```

2. **验证并清理死代码**:
   - 检查 `error-handler.ts`, `error-handling.ts`, `global-error-handlers.ts` 是否为遗留代码
   - 如确认无导入, 可删除

### 中优先级

3. **统一错误处理模块**: 考虑合并 `error/` 和 `errors/` 目录功能

---

## 📝 待办事项

- [ ] 确认 error-handler 相关文件是否为死代码
- [ ] 确认空目录是否可以删除
- [ ] 评估错误模块统一方案

---

## ✅ 完成项

- [x] 分析 src/lib/ 目录结构
- [x] 统计文件数: 629
- [x] 识别空目录
- [x] 检查重复文件和代码
- [x] 生成清理报告

---

**报告生成**: Executor 子代理