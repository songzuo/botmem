# TypeScript 错误修复报告

## 任务概述

- **日期**: 2026-03-29
- **项目**: 7zi-frontend
- **目标**: 修复剩余的 TypeScript 严格模式错误

## 已修复的错误

### 1. 测试文件类型错误

#### `src/app/api/mcp/rpc/__tests__/route.test.ts`

- 修复 `jsonrpc` 字段的类型为 `"2.0" as const`
- 影响的测试:
  - `should handle valid JSON-RPC request`
  - `should handle tools/list request`
  - `should handle tools/call request`
  - `should handle error responses from MCP server`
  - `should handle batch requests`

#### `src/app/api/notifications/__tests__/route.test.ts`

- 修复 Notification 类型定义，添加缺失的字段:
  - `message`
  - `read`
  - `createdAt`
- 修复 mock 数据返回类型，从 `mockResolvedValue('string')` 改为 `mockReturnValue(Notification)`

#### `src/app/api/users/__tests__/route.test.ts`

- 修复导入: `@jest/globals` → `vitest`
- 添加显式类型注解:
  - `resourceType: string`
  - `action: string`
  - `requirements: any`

#### `src/components/__tests__/OptimizedImage.test.ts`

- 修复导入路径: `../components/OptimizedImage` → `../OptimizedImage`

### 2. API 路由错误

#### `src/app/api/feedback/route.ts`

- 添加缺失的类型导入: `FeedbackFilter`, `FeedbackRating`
- 修复 rating 类型转换: `rating: rating as FeedbackRating`

#### `src/app/robots.ts`

- 移除 Next.js 15 中不支持的属性:
  - `crawlDelay`
  - `host`

## 剩余错误分类

### 高优先级 (影响生产代码)

#### 测试文件中的 jest 命名空间错误 (约 300+ 个错误)

```
Cannot use namespace 'jest' as a value.
```

影响文件:

- `src/app/api/a2a/jsonrpc/__tests__/route.test.ts`
- `src/app/api/a2a/queue/__tests__/route.test.ts`
- `src/app/api/a2a/registry/__tests__/route.test.ts`
- `src/app/api/auth/__tests__/route.test.ts`
- 其他多个测试文件

**说明**: 这些文件导入了 `jest` 但应该使用 `vitest`。

### 中优先级 (组件和页面)

#### `src/app/i18n-demo/page.tsx`

```
error TS2349: This expression is not callable.
Type 'TranslationFunctionWithI18n' has no call signatures.
```

**修复建议**: 检查 i18n 函数的导出和使用方式。

#### `src/components/OptimizedImage.tsx`

```
error TS2741: Property 'banner' is missing
error TS7006: Parameter implicitly has an 'any' type
error TS2322: Type '"base64"' is not assignable
```

**修复建议**:

- 添加 `banner` preset 或修改 ImagePreset 类型
- 为事件处理器参数添加类型注解

#### `src/components/ui/`

- `EmptyState.tsx`: Element to string 类型错误
- `Input.tsx`: prefix 属性类型冲突
- `LazyImage.tsx`: srcSet 属性不存在
- `TaskCard.tsx`: onMouseEnter 不支持
- `index.ts`: 导出的成员不存在

#### `src/app/image-optimization-demo/page.tsx`

```
Property 'loading' does not exist on type 'OptimizedImageProps'
```

#### `src/components/EnhancedPerformanceDashboard.tsx`

```
Property 'FID' does not exist on type 'WebVitalsMetrics'
```

**说明**: FID 已被 INP 替代。

#### `src/components/knowledge-lattice/KnowledgeLattice3D.tsx`

```
Operator '<' cannot be applied to types 'string | number' and 'number'
```

**修复建议**: 添加类型断言或守卫。

### 低优先级 (功能模块)

#### `src/features/auth/`

```
error TS2308: Module './lib/auth' has already exported a member
error TS2307: Cannot find module './validation'
error TS2352: Conversion of type 'JWTPayload' may be a mistake
```

#### `src/features/mcp/`

```
error TS2345: Argument not assignable to 'MCPResponse'
error TS2307: Cannot find module './lib/types'
error TS2339: Property 'execute' does not exist
```

#### `src/features/monitoring/`

```
error TS2308: Member already exported
error TS1205: Re-exporting a type requires 'export type'
error TS2420: Class incorrectly implements interface
error TS2393: Duplicate function implementation
```

#### `src/lib/`

```
error TS2307: Cannot find module '../db/storage'
```

## 统计

- **修复的错误**: 约 67 个
- **剩余错误**: 约 526 个
- **需要从 jest 迁移到 vitest 的文件**: 约 54 个测试文件
- **生产代码错误**: 约 227 个

### 错误分布

| 分类              | 数量 | 说明                            |
| ----------------- | ---- | ------------------------------- |
| jest 命名空间错误 | ~300 | 测试文件中使用 jest 而非 vitest |
| 生产代码类型错误  | ~227 | 组件、API、功能模块的类型问题   |

## 已修复的问题

### ✅ MCP RPC 测试

- 修复 `jsonrpc` 字段类型为 `"2.0" as const`
- 修复 OPTIONS 和 GET 方法调用，添加必需的 request 参数

### ✅ Notifications 测试

- 修复 Notification 类型定义
- 添加缺失的字段: message, read, createdAt
- 修复 mock 数据返回类型

### ✅ Users 测试

- 修复导入: `@jest/globals` → `vitest`
- 添加显式类型注解

### ✅ Feedback API

- 添加缺失的类型导入: `FeedbackFilter`, `FeedbackRating`
- 修复 rating 类型转换

### ✅ Robots.txt

- 移除 Next.js 15 不支持的属性: `crawlDelay`, `host`

## 建议后续步骤

1. **批量修复测试文件**: 使用脚本将 `jest` 替换为 `vitest`
2. **修复组件类型**: 逐个处理组件库的类型错误
3. **清理功能模块**: 修复 auth、mcp、monitoring 模块的类型问题
4. **移除废弃功能**: 如 FID 替换为 INP
5. **添加缺失的类型文件**: 如 `./validation`、`./lib/types`

## 结论

已成功修复部分测试文件的类型错误和几个生产代码的简单错误。剩余错误主要分为三类:

1. 测试框架迁移问题 (jest → vitest)
2. 组件类型定义不完整
3. 功能模块的架构问题

建议按优先级逐步修复，优先处理生产代码中的类型错误，测试文件可以暂时放宽检查或批量修复。
